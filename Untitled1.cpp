/*==============================================|
|			Author : Bijoy Chandra Nath         |
|			Created : 00/00/0000  00:00 AM      |
|			Problem Details :                   |
|==============================================*/
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <sstream>
#include <queue>
#include <deque>
#include <bitset>
#include <iterator>
#include <list>
#include <stack>
#include <map>
#include <set>
#include <functional>
#include <numeric>
#include <utility>
#include <limits>
#include <time.h>
#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>

using namespace std;

#define inputInt(t) scanf("%d",&t)
#define inputLong(t) scanf("%lld",&t)
#define inputChar(t) scanf("%c",&t)
#define SCS(t) scanf("%s",t)
#define inputFloat(t) scanf("%f",&t)
#define inputDouble(t) scanf("%lf",&t)
#define MP make_pair
#define PB push_back
#define F first
#define S second
#define ll long long
#define REP(i,a,b) for(int i=a;i<=b;i++)

typedef pair<int, int> PairIntInt;
typedef vector<int> VecInt;
typedef vector<string> VecString;
typedef vector<PairIntInt> VecIntInt;
typedef vector<VecInt> VecVecInt;
typedef map<int,int> MapIntInt;
typedef set<int> SetInt;
typedef multiset<int> MultisetInt;

const int MAX_N = 1e5 + 1;
const int MOD = 1e9 + 7;
const int INF = 1e9;
const ll LINF = 1e18;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    ll TestCase;
    inputLong(TestCase);
    while(TestCase--)
    {


    }
}